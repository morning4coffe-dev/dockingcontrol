<UserControl x:Class="DockingControlSample.DockingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:DockingControlSample"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

  <Grid x:Name="MainGrid">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>

    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <!-- will not be part of the control, move to mainpage -->
    <Button Content="Add New Panel"
            Grid.Row="0"
            Grid.Column="0"
            Click="OnAddPanelClicked"
            Margin="10" />

    <!-- this will be dynamic -->
    <local:DockArea x:Name="LeftDockArea"
                    Grid.Column="0"
                    Grid.Row="1" />

    <local:DockArea x:Name="CenterDockArea"
                    Grid.Column="1"
                    Grid.Row="1" />

    <local:DockArea x:Name="RightDockArea"
                    Grid.Column="2"
                    Grid.Row="1" />
  </Grid>
</UserControl>
